/*
 * Jenkinsfile for Notifer Plugin
 * Uses the standard Jenkins plugin build process
 */
buildPlugin(
    useContainerAgent: true,
    configurations: [
        [platform: 'linux', jdk: 11],
        [platform: 'linux', jdk: 17],
    ]
)
